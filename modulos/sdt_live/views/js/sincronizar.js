var calendar = $(document).ready(function() {
	$("input[id^='syn_ch_']").click(function(){
		// var index = $(this).attr("id");
		// index = index.split("_");
		// index = index[1];
		// var val = this.checked ? 1 : 0;
		// $.post(server+'/sdt_live/actualizarCalendario','dia='+$(div2).attr("id")+'&estado='+estado+"&fecha="+NewDate(dia-4), function(datos){
	        	

		// }, 'json');
	});
});